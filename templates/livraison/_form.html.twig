{{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}
    <div class="row">
        <div class="col-md-6">
            <div class="form-group mb-4">
                {{ form_label(form.estimated_delivery, '📅 Date de livraison estimée', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                <div class="input-group">
                    <span class="input-group-text"><i class="fas fa-calendar-alt"></i> 📅</span>
                    {{ form_widget(form.estimated_delivery, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="invalid-feedback">
                    {{ form_errors(form.estimated_delivery) }}
                </div>
            </div>
        </div>
        
        <div class="col-md-6">
            <div class="form-group mb-4">
                {{ form_label(form.delivery_cost, '💰 Coût de livraison (€)', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                <div class="input-group">
                    <span class="input-group-text"><i class="fas fa-euro-sign"></i> 💰</span>
                    {{ form_widget(form.delivery_cost, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="invalid-feedback">
                    {{ form_errors(form.delivery_cost) }}
                </div>
            </div>
        </div>
        
        <div class="col-md-6">
            <div class="form-group mb-4">
                {{ form_label(form.poids_colis, '⚖️ Poids du colis (kg)', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                <div class="input-group">
                    <span class="input-group-text"><i class="fas fa-weight-hanging"></i> ⚖️</span>
                    {{ form_widget(form.poids_colis, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="invalid-feedback">
                    {{ form_errors(form.poids_colis) }}
                </div>
            </div>
        </div>
        
        <div class="col-md-6">
            <div class="form-group mb-4">
                {{ form_label(form.destination_status, '🏷️ Statut de destination', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                <div class="input-group">
                    <span class="input-group-text"><i class="fas fa-tag"></i> 🏷️</span>
                    {{ form_widget(form.destination_status, {'attr': {'class': 'form-select'}}) }}
                </div>
                <div class="invalid-feedback">
                    {{ form_errors(form.destination_status) }}
                </div>
            </div>
        </div>
    </div>
    
    <div class="form-group mt-5 pt-3 border-top">
        <button type="submit" class="btn btn-primary me-3">
            <i class="fas fa-save me-1"></i> 💾 {{ button_label|default('Enregistrer') }}
        </button>
        <a href="{{ path('app_livraison_index') }}" class="btn btn-outline-secondary">
            <i class="fas fa-times me-1"></i> ❌ Annuler
        </a>
    </div>
{{ form_end(form) }}