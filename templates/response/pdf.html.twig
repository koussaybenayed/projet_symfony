<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Response Details - PDF Export</title>
    <style>
        body { font-family: Arial, sans-serif; }
        h1 { color: #333; }
        .header { text-align: center; margin-bottom: 20px; }
        .logo { max-width: 150px; }
        table { width: 100%; border-collapse: collapse; margin-bottom: 20px; }
        th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
        th { background-color: #f2f2f2; }
        .footer { margin-top: 30px; text-align: center; font-size: 0.8em; }
    </style>
</head>
<body>
    <div class="header">
        <h1>Response Details</h1>
        <p>Generated on: {{ "now"|date("Y-m-d H:i:s") }}</p>
    </div>

    <table>
        <tbody>
          
            <tr>
                <th>Response Text</th>
                <td>{{ response.responseText }}</td>
            </tr>
            <tr>
                <th>Created At</th>
                <td>{{ response.createdAt ? response.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <h2>Related Reclamation</h2>
    <table>
        <tbody>
          
            <tr>
                <th>Description</th>
                <td>{{ response.reclamation.description }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ response.reclamation.dateReclamation ? response.reclamation.dateReclamation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ response.reclamation.status }}</td>
            </tr>
        </tbody>
    </table>

    <div class="footer">
        <p>Generated by Your Application</p>
    </div>
</body>
</html>