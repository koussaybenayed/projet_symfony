{% extends 'base.html.twig' %}

{% block title %}Nouvelle Réduction{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="card shadow-lg border-0" style="background-color: #e6f0ff;"> {# bleu clair #}
        <div class="card-header text-white" style="background-color: #007bff;">
            <h2><i class="fas fa-tags"></i> 💸 Créer une Nouvelle Réduction</h2>
        </div>
        <div class="card-body">
            <p class="text-center text-muted">Entrez les informations de la nouvelle réduction dans le formulaire ci-dessous 📄</p>

            <div class="d-flex justify-content-center">
                <div class="col-md-8">
                    {{ include('reduction/_form.html.twig') }}
                </div>
            </div>

            <div class="text-center mt-4">
                <a href="{{ path('app_reduction_index') }}" class="btn btn-outline-primary">
                    <i class="fas fa-arrow-left"></i> Revenir à la liste des réductions
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
