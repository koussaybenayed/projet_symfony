{% extends 'base.html.twig' %}

{% block title %}➕ Nouveau contrôle douanier{% endblock %}

{% block body %}
    <div class="card mb-4" style="background-color: white;">
        <div class="card-header">
            <h1 class="h3 mb-0">➕ Créer un nouveau contrôle douanier</h1>
        </div>
        
        {{ include('controle_douanier/_form.html.twig') }}
        
        <div class="card-footer">
            <a href="{{ path('app_controle_douanier_index') }}" class="btn btn-secondary">
                <i class="fas fa-arrow-left me-1"></i> ↩️ Retour à la liste
            </a>
        </div>
    </div>
{% endblock %}