<form method="post" action="{{ path('app_controle_douanier_delete', {'id_controle': controle_douanier.id_controle}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce contrôle douanier ?');" class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ controle_douanier.id_controle) }}">
    <button type="submit" class="btn btn-danger">
        <i class="fas fa-trash-alt me-1"></i> Supprimer
    </button>
</form>